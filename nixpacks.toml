[phases.setup]
nixPkgs = ["php", "php83Packages.composer", "php83Extensions.mysqli", "php83Extensions.pdo", "php83Extensions.pdo_mysql"]

[phases.build]
cmds = []

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."